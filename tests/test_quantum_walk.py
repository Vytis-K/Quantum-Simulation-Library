# test_quantum_walks.py
"""
import pytest
from quantum_sim.quantum_walks import QuantumWalk

def test_initial_state():
    num_positions = 10
    qw = QuantumWalk(num_positions)
    assert qw.position_state[0] == 1
    assert all(qw.position_state[1:] == 0)
"""